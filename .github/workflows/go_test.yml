name: go test
on: [pull_request]

jobs:
  run:
    name: Run Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3.5.1
      - uses: aquaproj/aqua-installer@v1.2.0
        with:
          aqua_version: v2.2.1
      - run: go test ./...
