name: go test
on: [pull_request]

jobs:
  run:
    name: Run Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3.5.1
      - uses: aquaproj/aqua-installer@v2.1.1
        with:
          aqua_version: v2.3.4
      - run: go test ./...
