name: go test
on: [pull_request]

jobs:
  run:
    name: Run Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3.5.2
      - uses: aquaproj/aqua-installer@v2.1.1
        with:
          aqua_version: v2.8.0
      - run: go test ./...
