name: go test
on: [pull_request]

jobs:
  run:
    name: Run Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3.1.0
      - uses: aquaproj/aqua-installer@v1.1.2
        with:
          aqua_version: v2.2.1
      - run: go test ./...
